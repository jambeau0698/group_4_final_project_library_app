name: group_4_final_project_library_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.4
  shared_preferences: ^2.5.3
  reactive_forms: ^17.0.1
  intl: ^0.19.0
  sqflite: ^2.4.1
  path: ^1.9.0
  path_provider: ^2.0.15
  flutter_map: ^6.1.0
  latlong2: ^0.9.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
